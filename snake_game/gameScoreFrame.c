#include "gameScoreFrame.h"

#include "gameSystem.h"

void openGameScoreFrame(void)
{
	printSquare(GAME_SCORE_FRAME_X_START, GAME_SCORE_FRAME_Y_START, GAME_SCORE_FRAME_WIDTH, GAME_SCORE_FRAME_HEIGHT);
}